#include <stdio.h>
#include <stdlib.h>

int main(){
    // int idade = 16, resultado;

    //condição ? verdadeiro : falso
    
                // resultado = idade >= 18 ? 1 : 0;

                // if (resultado == 1){
                //     printf("Voce é maior de idade\n");

                // } else {
                //     printf("Voce é menor de idade\n");
                // }

            //  int temperatura = 10, resultado;

            //  resultado = temperatura >= 30 ? 1 : 0;

            //  if(resultado == 1){
            //      printf("Está calor\n");

            //  } else{

            //      printf("Está Frio\n");
            //  }

            int num1 = 40, num2 = 60, maior;

            num1 > num2 ? (maior = num1) : (maior = num2);
            printf("O maior numero é: %d\n", maior);


}
